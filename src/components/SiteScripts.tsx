// Lightweight placeholder to host global scripts or analytics in the root layout.
// You can extend this component to inject script tags as needed.
export default function SiteScripts() {
  return null;
}

